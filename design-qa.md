# 商城方案一 Design QA

## 对照基准

- 设计稿：`C:\Users\16794\.codex\generated_images\01a06a77-7080-7971-a80d-a8ee5ba2ba44\exec-02de8b79-4a17-4b26-bac1-23a923526eaf.png`
- 实现页：`http://127.0.0.1:5193/#/shop`
- 验收状态：PASSED
- final result: passed

## 视觉对照

- 延续方案一的暖象牙底色、编辑式留白、低饱和玫瑰色点缀和精品陈列感。
- 首屏保留“标题 / 积分 / 当前试穿 / 正在使用”的信息层级，真实宠物作为视觉焦点。
- 商品卡用真实边框素材和当前宠物实时预览，移除了边框商品中的符号占位表现。
- 当前试穿区改为 `220 × 320px` 的竖向宠物卡，替代原先偏横向的 `270 × 242px` 预览；移动端使用 `210 × 300px`。
- 试穿卡采用教师端宠物卡的信息结构：顶部宠物名与等级徽标、中部宠物舞台、底部饱食与成长进度；信息层置于装扮边框之上，避免被花叶遮挡。
- 四套边框以 ImageGen 的 `*-portrait-v2.png` 为母版，通过 `scripts/standardize-frame-assets.ps1` 生成统一几何的竖版 `*-portrait-v3.png` 和横版 `*-landscape-v3.png`；两种画布都把主线中线固定在 5% 位置。
- 所有卡片统一使用 `5.56%` 外扩公式（`5% / (1 - 2×5%)`），使素材缩放后边框线中线始终与卡片边缘重合，不再逐素材调 inset。
- 商品卡与商城试穿卡统一为 `220 × 320px`；教师端紧凑/标准/大字三档固定为 `180 × 350px`、`230 × 410px`、`300 × 480px`；学生主页卡固定高度 `500px` 并使用独立横版素材。
- 边框商品卡统一为 5:7 比例；宠物、商品名、价格与操作按钮均完整位于卡片内部，没有被装饰轨道遮挡。
- 背景主题铺满整张 `220 × 320px` 商品卡，预览区不再叠加内层浅色底，宠物、文案、价格和按钮直接落在连续主题中；深色主题使用高对比文字，“隐藏背景”保留干净白底。
- 商城商品卡间距扩大到横向 `62px`、纵向 `64px`，教师端卡片间距扩大到 `62px`；操作按钮增加底部安全距离，避免外侧角饰压邻卡、内侧角饰压按钮。
- 960px 以下切换双列；390×844 移动端已实机视口截图检查，商品信息和操作按钮无横向溢出。

## 功能与质量检查

- 卡片边框、背景主题、购买记录三个页签均已在真实页面数据下验证。
- 教师端普通视图不额外占用操作按钮，直接点击宠物主体即可打开装扮面板；可查看学生积分和拥有状态，使用学生积分代购并立即装备，也可更换或卸下。数据库强制校验师生归属并记录实际操作教师。
- 已拥有、使用中、购买并使用、立即装备、卸下等状态均保留。
- 购买记录使用商品对应装扮缩略图，不再显示 emoji 商品图标。
- 浏览器控制台：0 条 warning / error。
- `vue-tsc -b`：通过。
- `vite build`：通过。
- `git diff --check`：通过（仅有 Git 的 CRLF 提示）。

## 已知边界

- 本轮没有触发真实购买，以免修改当前账号积分；购买与装备逻辑沿用现有 Supabase RPC 路径。
- 视觉验收使用当前真实学生账号与宠物数据完成。
